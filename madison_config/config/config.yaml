# snpArcher v2 Configuration Example

samples: "config/samples.csv"

# Optional: per-sample metadata for modules (exclude, outgroup, lat, long, etc.)
sample_metadata: "config/sample_metadata.csv"

reference:
  name: "mLoxAfr1.hap2"
  source: "/scratch/gpfs/CAMPBELLSTATON/ml9889/reference_genomes/genomes/GCF_030014295.1_mLoxAfr1.hap2_genomic.fna"  # Can be a refseq/genbank accession, url, or path

variant_calling:
  expected_coverage: "high"  # low | high | auto (future)
  tool: "gatk" # gatk | sentieon | bcftools | deepvariant | parabricks
  ploidy: 2
  gatk:
    het_prior: 0.005
  sentieon:
    license: ""
  bcftools:
    min_mapq: 20
    min_baseq: 20
    max_depth: 250
  deepvariant:
    model_type: "WGS"
    num_shards: 8
  parabricks:
    container_image: ""  # Required if tool == "parabricks"
    num_gpus: 1
    num_cpu_threads: 16
    extra_args: ""

intervals:
  enabled: true
  min_nmer: 500
  num_gvcf_intervals: 50
  db_scatter_factor: 0.15
  min_contig_length: 0
  db_max_intervals_per_shard: 200
  db_max_contigs_per_shard: 200


callable_sites:
  generate_bed_file: True
  coverage:
    enabled: true
    fraction: 1.0
    min_coverage: auto
    max_coverage: auto
    merge_distance: 100
  mappability:
    enabled: true
    min_score: 1
    kmer: 150
    merge_distance: 100

modules:
  qc:
    enabled: true
    clusters: 20
    min_depth: 10
    google_api_key: ""
    exclude_scaffolds: ""
  postprocess:
    enabled: true
    filtering:
      contig_size: 10000000
      maf: 0.01
      missingness: 0
      exclude_scaffolds: "NC_087370.1,NC_087369.1"
